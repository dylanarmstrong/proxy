plugins:
  - serverless-offline

service: proxy

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1

functions:
  proxy:
    handler: handler.proxy
    events:
      - http:
         path: ''
         method: get
         cors: true
